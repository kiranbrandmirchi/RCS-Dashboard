/**
 * Static login credentials (no backend).
 * Replace with API auth when backend is connected.
 */
export const STATIC_CREDENTIALS = {
  username: 'admin',
  password: 'admin123',
};

export const AUTH_STORAGE_KEY = 'wow_dashboard_auth';
